spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL???????
    url: jdbc:mysql://60.204.241.30:3306/open-api?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC # MySQL?????URL????????????????
    username: root  # MySQL??????
    password: biaoge666  # MySQL?????
    # ?????
    hikari:
      minimum-idle: 10
      maximum-pool-size: 10
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000
      auto-commit: true

  application:
    # 服务名称
    name: api-ws
  cloud:
    nacos:
      discovery:
        # nacos注册中心地址
        server-addr: 106.54.9.19:8848

  profiles:
    active: @profiles.active@


server:
  port: 9999

jwt:
  # JWT存储的请求头
  tokenHeader: Authorization
  # JWT 加解密使用的密钥
  secret: ledger
  # JWT的超期限时间（60*60*24）
  expiration: 604800
  # JWT 负载中拿到开头
  tokenHead: Bearer

